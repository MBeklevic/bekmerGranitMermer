# Active Context

## Current focus
- Polish branding (primary color + brand name), deployment.

## Recent changes
- Created core Memory Bank files: projectbrief, productContext, systemPatterns, techContext.
- Scaffolded Next.js App Router project with Tailwind.
- Implemented pages in Turkish: `/`, `/hakkimizda`, `/hizmetler`, `/referanslar`, `/iletisim`, `/teklif-al`.
- Added shared `Header`, `Footer`, and `ThemeToggle` (dark mode).
- Enabled static export, remote image domain, and path aliases.
- Pinned ESLint v8 to resolve dependency conflict.
- Removed client `onSubmit` handlers from server components (forms remain static).
- Updated `.gitignore` (`node_modules`, `.next`, `out`, etc.).

## Next steps
1. Confirm brand name and color palette unification (gold vs blue).
2. Add any final SEO meta (OG image, canonical) if provided.
3. Prepare deployment (Vercel or static hosting).

## Decisions pending
- Consistent primary color (gold vs blue) across pages.
- Brand name replacement for "Marble Co." if desired.
- Deployment target and domain.


